import { authorizationSlice } from './model/slice';
export const authorizationActions = authorizationSlice.actions;
export const authorizationSelectors = authorizationSlice.selectors;

export * from './api';
